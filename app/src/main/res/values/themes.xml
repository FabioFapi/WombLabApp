<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme usando AppCompat -->
    <style name="Theme.WombLab" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="colorPrimary">@color/womblab_primary</item>
        <item name="colorPrimaryDark">@color/womblab_primary_dark</item>
        <item name="colorAccent">@color/womblab_accent</item>
        <item name="android:statusBarColor">@color/womblab_primary</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.WombLab.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/Theme.WombLab</item>
    </style>

</resources>